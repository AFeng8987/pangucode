<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.linlinjava.litemall.db.dao.GoodsActivityMapper">
    <resultMap id="BaseResultMap" type="org.linlinjava.litemall.db.domain.LitemallGoodsActivity">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="activity_id" jdbcType="INTEGER" property="activityId" />
        <result column="goods_id" jdbcType="INTEGER" property="goodsId" />
        <result column="add_time" jdbcType="TIMESTAMP" property="addTime" />
        <result column="deleted" jdbcType="BIT" property="deleted" />
    </resultMap>


    <select id="list" resultType="java.lang.String">
        select goods_id from litemall_goods_activity
        <where>
            deleted = 0 and activity_id = #{activityId}
            <if test="goodsId != null and goodsId != ''">
                and goods_id = #{goodsId}
            </if>
            <if test="goodsName != null and goodsName != ''">
                and goods_name= #{goodsName}
            </if>
            order by add_time desc
        </where>
    </select>

    <delete id="del">
        delete from litemall_goods_activity
        where activity_id = #{activityId,jdbcType=INTEGER} and goods_id = #{goodsId,jdbcType=INTEGER}
    </delete>

    <delete id="delList">
        delete from litemall_goods_activity where
        activity_id = #{activityId,jdbcType=INTEGER}
        and goods_id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <insert id="addList" parameterType="java.util.List">
      <selectKey resultType ="java.lang.Integer" keyProperty= "id"
           order= "AFTER">
          SELECT LAST_INSERT_ID()
      </selectKey >
      insert into litemall_goods_activity(activity_id, goods_id, add_time) values
        <foreach collection="list" index="index" item="item" separator=",">
            (
             #{item.activityId,jdbcType=INTEGER},
             #{item.goodsId,jdbcType=INTEGER},
             #{item.addTime,jdbcType=TIMESTAMP}
            )
        </foreach>
    </insert>
</mapper>
